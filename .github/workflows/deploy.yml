name: Publish Website

on:
  push:
    branches:
      - dev

jobs:
  web-deploy:
    name: 🚀 Deploy Website
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get Latest Code
        uses: actions/checkout@v4

      - name: 📂 Sync Files
        uses: SamKirkland/web-deploy@v1
        with:
          target-server: 145.79.212.92
          remote-user: u488451890
          private-ssh-key: ${{ secrets.SSH_KEY }}
          ssh-port: 65002
          destination-path: /home/u488451890/domains/staging.sarathiailabs.com/public_html/wp-content/themes/custom-theme/