{
	"name": "custom-theme",
	"version": "1.0.0",
	"description": "A clean WordPress starter theme for custom website development.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "vendor/bin/phpcs",
		"lint:fix": "vendor/bin/phpcbf",
		"prepare": "husky install"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^17.3.0"
	},
	"lint-staged": {
		"*.php": [
			"vendor/bin/phpcbf --standard=WordPress --encoding=utf-8",
			"git add"
		]
	}
}